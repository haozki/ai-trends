# 2026-06-17 工作日志

## Astro 项目迁移

- 从零搭建 Astro 5 静态站点框架
- 删除 GitHub Actions 工作流，改用本地构建方案
- 创建 Hello World 页面
- 部署流程：`npm run build` → `git subtree split --prefix=dist -b gh-pages-deploy` → `git push origin gh-pages-deploy:gh-pages --force`
- 更新 AGENTS.md 记录新的项目结构和部署流程

## 当前状态

- Hello World 页面已部署到 gh-pages 分支
- 网站地址：https://haozki.github.io/ai-trends/

## Astro 内容集合配置

- 创建 `src/content.config.ts` 定义博客集合 Schema
- 创建 `src/content/blog/codex-introduction.md` 示例文章（OpenAI Codex 介绍）
- 创建 `src/pages/blog/[...id].astro` 动态路由页面
- 创建 `src/pages/blog/index.astro` 博客列表页面
- 更新首页添加博客入口链接
- 更新 git-history.html 记录最新提交

## 部署状态

- 本地构建成功，生成 3 个页面
- GitHub 推送失败（SSL/TLS 连接问题）
- 需要手动部署：`npm run build && git subtree split --prefix=dist -b gh-pages-deploy && git push origin gh-pages-deploy:gh-pages --force && git branch -D gh-pages-deploy`

## 最终状态

- ✅ 所有文件已创建并提交
- ✅ 本地构建成功
- ❌ GitHub 推送失败（网络问题）
- 📝 已更新长期记忆 MEMORY.md

## 路径修复

- 修改 astro.config.mjs，将 base 从 `/ai-trends/` 改为 `./`
- 首页链接从 `/ai-trends/blog/` 改为 `./blog/`
- 博客列表页链接从 `/ai-trends/blog/${post.id}` 改为 `./${post.id}`
- 文章详情页返回链接从 `/ai-trends/` 改为 `../../`
- 确保本地直接打开和线上 GitHub Pages 二级目录部署均能正确工作
- 更新 git-history.html 记录最新提交

## dist 目录文件缺失问题

- 问题：.gitignore 中有 `dist/`，导致 dist 目录下的文件被忽略
- 现象：gh-pages 分支上只有 index.html，缺少 blog 子目录和 CNAME
- 解决：使用 `git add -f dist/` 强制添加 dist 目录下的所有文件
- 已提交并重新部署到 gh-pages 分支
- 验证：gh-pages 分支现在包含 CNAME、blog/codex-introduction/index.html、blog/index.html、index.html

## 远程 master 分支同步问题

- 问题：远程 master 分支被清空，本地 master 分支有大量提交
- 解决：使用 `git push origin master --force` 强制推送本地 master 到远程
- 验证：远程 master 分支现在包含所有文件

## 最终状态

- ✅ 所有文件已创建并提交
- ✅ 本地构建成功
- ✅ 路径问题已修复
- ✅ GitHub 推送成功
- ✅ gh-pages 分支包含所有文件
- ✅ master 分支已同步到远程
- ✅ .gitignore 已更新，移除 dist/
- 📝 已更新长期记忆 MEMORY.md

## 验证结果

- gh-pages 分支：CNAME、blog/codex-introduction/index.html、blog/index.html、index.html
- master 分支 dist 目录：dist/CNAME、dist/blog/codex-introduction/index.html、dist/blog/index.html、dist/index.html

## 部署命令

```bash
npm run build && git subtree split --prefix=dist -b gh-pages-deploy && git push origin gh-pages-deploy:gh-pages --force && git branch -D gh-pages-deploy
```

## 项目级别 Skills 安装

- 在项目目录下安装了三个项目级别 skills：
  1. **frontend-design**：复制自用户级别的 impeccable skill，用于创建高质量前端界面
  2. **web-design-guidelines**：从 SkillHub 下载，用于审查 UI 代码是否符合 Web 界面规范
  3. **ui-ux-pro-max**：从 SkillHub 下载，提供 UI/UX 设计智能与实现指导
- 所有 skills 安装在 `D:\项目\AIBOX\.workbuddy\skills\` 目录下，仅对当前项目生效
- 安装前进行了安全审计，检查 SKILL.md 和脚本文件，未发现恶意代码
- 验证：三个 skills 目录均已创建，包含完整的 SKILL.md 和相关资源文件

## AI科技极简风格改造

- 使用 ui-ux-pro-max skill 生成设计系统，确定配色方案、字体、布局等规范
- 配色方案：浅紫色背景 (#FAF5FF) + 紫色主色 (#7C3AED) + 青色强调 (#06B6D4)
- 字体：Space Grotesk（标题）+ DM Sans（正文）
- 全局样式改造：更新CSS变量、添加字体导入、优化全局样式
- 首页改造：英雄区域、特色卡片、页脚，添加微交互动效
- 博客页面改造：列表页和文章详情页，保持风格一致
- 响应式设计：适配移动端、平板、桌面端
- 构建成功，开发服务器运行在 http://localhost:4321/
- 服务器已停止，项目已准备就绪
- 所有任务完成，项目已成功改造为AI科技极简风格

## 极简结构改造

- 用户要求首页直接显示博客列表，实现极简结构
- 将首页改为博客列表页，移除英雄区域和特色卡片
- 更新文章详情页的返回链接，从 ../../ 改为 /
- 保留博客列表页（/blog/）作为备用入口
- 构建成功，首页现在直接显示博客文章列表
# 2026-06-15 工作记录

- 热门模型页面"生图"菜单改名为"生图片"
- 新增"每日分享"tab 页面，展示 10 个 AI 产品卡片（ChatGPT、Claude、HeyGen、可灵、Kimi、Midjourney、Perplexity、Runway、Suno、通义千问）
- 每日分享页面：卡片网格布局 + 详情浮层（含封面图、logo、功能亮点、截图、用户评价、上手指南、编辑点评）
- 项目资源从 ProjectsCollection/asset/ 复制到 dist/projects/ 供静态站点引用
- 修复：添加每日分享时误删 `<script>` 标签，导致 JS 代码暴露为 HTML 文本
- 修复：dailyProjects 数据中 editorNote/desc 含内嵌 ASCII 双引号导致 JS 语法错误，已替换为中文引号""
- 调整（已回退）：尝试改用独立路径访问 + 去掉卡片 Logo，用户不喜欢，已 git reset --hard 回退
- 回退：用户要求回退到 8cbb0ee（弹窗版），git reset --hard 丢弃 4061b3e 和 709d7de 两次 commit
- 教训：commit 后必须预览并确认再继续，不要"我以为 OK"就停手
- 重新修复：reset --hard 同时丢掉了双引号修复 commit，再次修复并提交（13 处）
